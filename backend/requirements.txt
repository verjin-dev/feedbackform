fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
email-validator==2.2.0
pydantic-settings==2.7.0
psycopg[binary]==3.2.3
argon2-cffi==23.1.0
pyjwt==2.10.1
python-multipart==0.0.20
segno==1.6.6

# Runtime, not just tests: college sign-in calls the identity provider's
# discovery and token endpoints, and PyJWT needs cryptography to verify the
# RS256 signature on an ID token.
httpx==0.28.1
cryptography==50.0.0

# Development and test
pytest==8.3.4
