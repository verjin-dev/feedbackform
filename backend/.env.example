ENVIRONMENT=local
DEBUG=true
# Postgres for real use; the sqlite URL below is only for a quick local smoke test.
DATABASE_URL=postgresql+psycopg://evaluation:changeme@localhost:5432/evaluation
SECRET_KEY=insecure-local-only-key
CORS_ORIGINS=["http://localhost:5173"]
